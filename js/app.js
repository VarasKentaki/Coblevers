fetch('data/pokemon.json').then(r=>r.json()).then(data=>{const g=document.getElementById('grid'),s=document.getElementById('search'),sort=document.getElementById('sort');document.getElementById('totalPokemon').textContent=data.length+' Pokémon';let drops=new Set();data.forEach(p=>(p.drops||'').split(',').forEach(d=>drops.add(d.trim())));document.getElementById('totalDrops').textContent=drops.size+' Drops';
function render(){let list=[...data];const q=s.value.toLowerCase();if(q)list=list.filter(p=>JSON.stringify(p).toLowerCase().includes(q));if(sort.value==='name')list.sort((a,b)=>a.name.localeCompare(b.name));g.innerHTML='';list.forEach(p=>{let c=document.createElement('div');c.className='card';c.innerHTML=`<img class="pokemon-img" src="https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/other/official-artwork/${p.num}.png"><h3>#${p.num} ${p.name}</h3><p>${p.drops||'Sin drops'}</p><button class='btn'>Detalles</button>`;c.querySelector('button').onclick=()=>{document.getElementById('modalBody').innerHTML=`<h2>${p.name}</h2><img class="pokemon-img" src="https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/other/official-artwork/${p.num}.png"><p><b>Drops:</b> ${p.drops}</p><p><b>Especial:</b> ${p.special||'Ninguno'}</p><h3>Evoluciones</h3><p>Función preparada para futuras cadenas evolutivas.</p>`;document.getElementById('modal').classList.remove('hidden');};g.appendChild(c);});}
document.getElementById('close').onclick=()=>document.getElementById('modal').classList.add('hidden');s.oninput=render;sort.onchange=render;render();});